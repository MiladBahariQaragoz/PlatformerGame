# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build output
dist/
build/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Logs & temp
*.log
*.tmp
.cache/

# Local Claude settings are kept; ignore secrets only
.env
.env.local

# Source images for the face SVGs: the game uses the generated assets/images/*.svg (the raster
# is embedded inside them), so these large originals don't need to be in the repo.
assets/images/hero.png
assets/images/chaser.jpg
